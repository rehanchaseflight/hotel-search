const { chromium } = require('playwright');
const { decrypt } = require('../crypto-util');
const clean = v => String(v ?? '').replace(/\s+/g, ' ').trim();
const PRICE_RE=/(?:AED|SAR|USD|EUR|GBP|PKR|US\$|\$)\s*[0-9][0-9,]*(?:\.[0-9]{1,2})?|\b[0-9]{2,6}\.[0-9]{2}\b/i;
const LOGIN_FRAME = 'https://iolglobalb2bcloudssl.iolcloud.com/login.aspx?SourceXid=MTE3NTNTM=';
const SEARCH_PAGE = 'https://iolglobalb2bcloudssl.iolcloud.com/HotelSearch.aspx?CallFrom=AWBE';
function date(v) { const m = String(v || '').match(/^(\d{4})-(\d{2})-(\d{2})$/); return m ? `${m[3]}/${m[2]}/${m[1]}` : String(v || ''); }
async function bodyText(frame) { return clean(await frame.locator('body').innerText().catch(() => '')); }
async function blocked(page) { for (const f of page.frames()) if (/captcha|verify you are human|access denied|unusual traffic|security check/i.test(await bodyText(f))) throw new Error('Supplier presented a security verification step; automated bypass is not supported'); }
async function loggedIn(page) { let t = ''; for (const f of page.frames()) t += ' ' + await bodyText(f); return /welcome\b|\blogout\b/i.test(t); }
async function findLoginFrame(page) { for (const f of page.frames()) if (await f.locator('#tbUserName').count().catch(() => 0)) return f; return page.frames().find(f => /login\.aspx/i.test(f.url())) || null; }
async function login(page, source, password, cfg) { let frame = null; const end = Date.now() + (Number(cfg.login_frame_timeout_ms) || 45000); while (!frame && Date.now() < end) { frame = await findLoginFrame(page); if (!frame) await page.waitForTimeout(500); } if (!frame) { try { await page.goto(cfg.login_frame_url || LOGIN_FRAME, { waitUntil: 'commit', timeout: 20000 }); await page.waitForTimeout(1500); } catch {} frame = await findLoginFrame(page); } if (!frame) { if (await loggedIn(page)) return; throw new Error('Hadaf login iframe could not be detected'); } await frame.locator('#tbUserName').fill(String(source.site_username || '')); await frame.locator('#tbPassword').fill(String(password || '')); const terms = frame.locator('#chkTermCondn').first(); if (await terms.count().catch(() => 0) && !(await terms.isChecked().catch(() => false))) await terms.check().catch(() => {}); await frame.locator('#btnLogin1').click({ timeout: 10000 }); await page.waitForTimeout(Number(cfg.post_login_wait_ms) || 7000); await blocked(page); if (!await loggedIn(page)) throw new Error('Hadaf login was submitted but the portal did not show a logged-in state'); }
async function openHadaf(source) { const cfg = source.browser_config || {}; if (!source.login_url || !source.site_username || !source.site_password_enc) return { configured:false, live:false, error:'Hadaf credentials are not configured' }; let password; try { password = decrypt(source.site_password_enc); } catch (e) { return { configured:true, live:false, error:`Credential decryption failed: ${e.message}` }; } let browser, context; try { browser = await chromium.launch({ headless:true }); context = await browser.newContext({ viewport:{width:1440,height:1000} }); const page = await context.newPage(); page.setDefaultTimeout(Number(cfg.timeout_ms)||15000); await page.goto(source.login_url,{waitUntil:'commit',timeout:60000}); await page.waitForTimeout(Number(cfg.initial_wait_ms)||3000); await blocked(page); await login(page,source,password,cfg); return {configured:true,live:true,browser,context,page}; } catch(e) { if(context) await context.close().catch(()=>{}); if(browser) await browser.close().catch(()=>{}); return {configured:true,live:false,error:e.name==='TimeoutError'?`Hadaf browser timed out: ${e.message}`:e.message}; } }
async function healthHadafSource(source) { const r=await openHadaf(source); if(r.context) await r.context.close().catch(()=>{}); if(r.browser) await r.browser.close().catch(()=>{}); return {configured:r.configured,live:r.live,error:r.error||null}; }
async function searchForm(page) { const end=Date.now()+30000; while(Date.now()<end){ for(const f of page.frames()){ const ok=await f.locator('#lpPannel_txtV5City').count().catch(()=>0); const form=await f.locator('#lpPannel_txtFromDate').count().catch(()=>0); if(ok&&form)return f; } await page.waitForTimeout(300); } return null; }
async function openSearchPage(page,cfg){ if(/HotelSearch\.aspx/i.test(page.url()))return; await page.goto(cfg.search_page_url||SEARCH_PAGE,{waitUntil:'domcontentloaded',timeout:Number(cfg.search_page_timeout_ms)||30000}); await page.waitForTimeout(Number(cfg.search_page_wait_ms)||2500); await blocked(page); }
async function chooseDestination(frame,value){ const desired=clean(value); const input=frame.locator('#lpPannel_txtV5City').first(); if(!await input.count().catch(()=>0)||!await input.isVisible().catch(()=>false)) throw new Error('Hadaf visible destination city input could not be detected'); await input.scrollIntoViewIfNeeded().catch(()=>{}); await input.fill(desired); await frame.waitForTimeout(1000); const escaped=desired.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'); const options=frame.locator('li:visible,[role="option"]:visible').filter({hasText:new RegExp(escaped,'i')}); if(await options.count().catch(()=>0)) await options.first().click().catch(()=>{}); else { await input.press('ArrowDown').catch(()=>{}); await input.press('Enter').catch(()=>{}); } await frame.waitForTimeout(700); const visibleValue=await input.inputValue().catch(()=>null); if(!visibleValue || !/makkah|medina|madinah|riyadh|jeddah|saudi/i.test(visibleValue)) throw new Error(`Hadaf destination selection did not stick. Visible value: ${JSON.stringify(visibleValue)}`); }
async function fillSearch(frame,search){ const country=frame.locator('#lpPannel_sel_nationality'); if(await country.count()) await country.selectOption({label:String(search.country||'United States of America')}).catch(()=>{}); await chooseDestination(frame,search.destination); await frame.locator('#lpPannel_txtFromDate').fill(date(search.checkin)); await frame.locator('#lpPannel_txtToDate').fill(date(search.checkout)); await frame.locator('#lpPannel_txtNights').fill(String(Math.max(1,Number(search.nights||1)))); await frame.locator('#sel_NoOfRooms').selectOption(String(search.rooms||1)).catch(()=>{}); await frame.locator('#sel_NoOfAdult_1').selectOption(String(search.guests||2)).catch(()=>{}); await frame.locator('#sel_NoOfChild_1').selectOption(String(search.children??0)).catch(()=>{}); if(search.hotel_name) await frame.locator('#lpPannel_txtHotel').fill(String(search.hotel_name)); await frame.locator('#lpPannel_txtFromDate').press('Tab').catch(()=>{}); await frame.locator('#lpPannel_txtToDate').press('Tab').catch(()=>{}); }
async function controlSummary(frame){ return frame.locator('input:visible,button:visible,a:visible').evaluateAll(els=>els.slice(0,100).map(e=>({tag:e.tagName,type:e.type||'',id:e.id||'',name:e.name||'',value:e.value||'',alt:e.alt||'',title:e.title||'',text:(e.innerText||'').trim().slice(0,80),src:e.src||''}))).catch(()=>[]); }
async function clickSearch(frame,cfg){ const exact=frame.locator('a#lpPannel_btnModifySearch').first(); if(await exact.count().catch(()=>0)&&await exact.isVisible().catch(()=>false)&&/^search$/i.test(clean(await exact.innerText().catch(()=>'')))){ await exact.click({timeout:10000,noWaitAfter:true}); return 'lpPannel_btnModifySearch'; } if(cfg.search_button_selector){ const x=frame.locator(cfg.search_button_selector).first(); if(await x.count().catch(()=>0)&&await x.isVisible().catch(()=>false)){ await x.click({timeout:10000,noWaitAfter:true}); return cfg.search_button_selector; } } const controls=frame.locator('a:visible,input:visible,button:visible'); const n=await controls.count().catch(()=>0); for(let i=0;i<n;i++){ const x=controls.nth(i); const meta=await x.evaluate(e=>({tag:e.tagName,type:e.type||'',id:e.id||'',name:e.name||'',value:e.value||'',alt:e.alt||'',title:e.title||'',text:(e.innerText||'').trim(),src:e.src||''})).catch(()=>null); if(!meta)continue; const label=Object.values(meta).join(' '); if(/logout|modify|back|reset|clear/i.test(label)&&meta.id!=='lpPannel_btnModifySearch')continue; if(meta.tag==='A'&&/^search$/i.test(clean(meta.text))){await x.click({timeout:10000,noWaitAfter:true});return meta.id||'link:Search';} if((meta.type==='image'||meta.type==='button'||meta.type==='submit')&&/search/i.test(label)){await x.click({timeout:10000,noWaitAfter:true});return meta.id||meta.name||meta.type;} } throw new Error(`Hadaf search button could not be detected. Controls: ${JSON.stringify(await controlSummary(frame))}`); }
function parseHadafRates(payload,cfg={}){ const out=[]; const hotels=Array.isArray(payload)?payload:(payload&&Array.isArray(payload.d)?payload.d:[payload]); for(const hotel of hotels){if(!hotel||typeof hotel!=='object')continue; const hotelName=clean(hotel.n||hotel.hotelName||hotel.HotelName||''); for(const group of Array.isArray(hotel.rlst)?hotel.rlst:[]){for(const rate of Array.isArray(group&&group.rdlst)?group.rdlst:[]){if(!rate||typeof rate!=='object')continue; const price=Number(rate.br??rate.gr??rate.r); if(!Number.isFinite(price)||price<=0)continue; const refundable=String(rate.p??'').toUpperCase()!=='N'&&String(rate.bo??'').toUpperCase()!=='N'; out.push({hotel:hotelName,room:clean(rate.rt||''),view:clean(rate.view||rate.roomView||''),board:clean(rate.mp||''),cancellation:refundable?clean(rate.hTCanx||''):'Non-refundable',price,currency:clean(rate.cc||rate.scc||cfg.default_currency||'AED'),availability:String(rate.sts||'').toUpperCase()==='A'?'Available':clean(rate.sts||''),supplier:'Hadaf Holidays',supplierRoomCode:clean(rate.ThirdPartyRoomCode||''),rateFrom:clean(rate.fd||''),rateTo:clean(rate.td||'')});}}} const seen=new Set(); return out.filter(r=>{const k=[r.hotel,r.room,r.view,r.board,r.cancellation,r.price,r.currency,r.availability,r.supplierRoomCode,r.rateFrom,r.rateTo].join('|');if(seen.has(k))return false;seen.add(k);return true;}); }
function parseRateText(text,def='AED'){ const t=clean(text),m=t.match(PRICE_RE); if(!m)return null; const num=Number(clean(m[0]).replace(/,/g,'').match(/([0-9]+(?:\.[0-9]{1,2})?)/)?.[1]); if(!Number.isFinite(num)||num<=0)return null; const currency=(m[0].match(/AED|SAR|USD|EUR|GBP|PKR|US\$|\$/i)||[def])[0]; return {price:num,currency:clean(currency),room:clean((t.match(/(?:Triple|Twin|Double|Single|Quadruple|Family|Deluxe|Standard|Superior|King|Queen|Suite|Apartment|Villa)[^|]*?(?:Room|Suite|Bed|Only|Included)?/i)||[''])[0]),board:clean((t.match(/Room Only|Breakfast Included|Bed and Breakfast|Half Board|Full Board|All Inclusive/i)||[''])[0]),cancellation:clean((t.match(/Non[- ]?refundable|Free Cancellation|Refundable/i)||[''])[0]),availability:clean((t.match(/Available|On Request|Sold Out|Not Available/i)||['Available'])[0]),raw:t}; }
function hotelLike(text){ const t=clean(text); return Boolean(t&&t.length>=4&&t.length<=180&&!PRICE_RE.test(t)&&/hotel|resort|suites|residence|makkah|madinah|riyadh|jeddah/i.test(t)); }
async function extractRenderedRates(context,cfg={}){ const out=[]; for(const page of context.pages()){ for(const frame of page.frames()){ try{
      const priceLinks=frame.locator('a:visible[href*="HotelResults.aspx#RatePopup"]');
      const count=await priceLinks.count().catch(()=>0);
      for(let i=0;i<count;i++){
        const link=priceLinks.nth(i);
        const priceText=clean(await link.innerText().catch(()=>''));
        const parsed=parseRateText(priceText,cfg.default_currency||'AED');
        if(!parsed||parsed.price<=0)continue;
        const rowText=clean(await link.evaluate(el=>{let n=el;for(let i=0;i<8&&n;i++,n=n.parentElement){const t=(n.innerText||n.textContent||'').replace(/\s+/g,' ').trim();if(t&&/Room Only|Breakfast|Half Board|Full Board|All Inclusive|Non-refundable|Package Rate|AED|SAR|USD/i.test(t))return t;}return el.innerText||'';}).catch(()=>priceText));
        const roomMatch=rowText.match(/(?:Double|Triple|Twin|Single|Quadruple|Family|Deluxe|Standard|Superior|King|Queen|Suite|Apartment|Villa)[^|]*?(?:Room|Suite|Bed|Only|Included)?/i);
        parsed.room=clean(roomMatch?roomMatch[0]:parsed.room);
        parsed.board=clean((rowText.match(/Room Only|Breakfast Included|Bed and Breakfast|Half Board|Full Board|All Inclusive/i)||[''])[0]);
        parsed.cancellation=clean((rowText.match(/Non[- ]?refundable|Free Cancellation|Refundable/i)||[''])[0]);
        parsed.availability=clean((rowText.match(/Available|On Request|Sold Out|Not Available/i)||['Available'])[0]);
        parsed.packageRate=/Package Rate/i.test(rowText);
        let hotel='';
        try{hotel=clean(await link.evaluate(el=>{let root=el.closest('div[id],section,article,fieldset')||el.parentElement;let cur=root;for(let i=0;i<8&&cur;i++,cur=cur.parentElement){const hs=cur.querySelectorAll('h1,h2,h3,h4,.hotelName,.hotel-name,.hotel-title');for(const h of hs){const t=(h.innerText||'').replace(/\s+/g,' ').trim();if(t&&!/room|price|search/i.test(t))return t;}}return '';}));}catch{}
        out.push({hotel, ...parsed});
      }
      if(!count){
        const body=await frame.locator('body').innerText().catch(()=>''), lines=String(body).split(/\r?\n/).map(clean).filter(Boolean);
        for(let i=0;i<lines.length;i++){ if(!PRICE_RE.test(lines[i]))continue; const parsed=parseRateText(lines.slice(Math.max(0,i-5),Math.min(lines.length,i+6)).join(' | '),cfg.default_currency||'AED'); if(!parsed)continue; let hotel=''; for(let j=i-1,steps=0;j>=0&&steps<18;j--,steps++) if(hotelLike(lines[j])){hotel=lines[j];break;} out.push({hotel,...parsed}); }
        const rows=await frame.locator('tr:visible').evaluateAll(rs=>rs.map(x=>clean(x.innerText||x.textContent||'')).filter(t=>t&&PRICE_RE.test(t))).catch(()=>[]); for(const row of rows){const parsed=parseRateText(row,cfg.default_currency||'AED');if(parsed)out.push({hotel:'',...parsed});}
      }
    }catch{} } } const seen=new Set(); return out.filter(r=>Number.isFinite(r.price)&&r.price>0).filter(r=>{const k=[clean(r.hotel),clean(r.room),clean(r.board),r.price,clean(r.currency),clean(r.cancellation),!!r.packageRate].join('|');if(seen.has(k))return false;seen.add(k);return true;}).slice(0,Number(cfg.max_results)||500); }
async function searchHadafSource(source,search){ const cfg=source.browser_config||{}; if(!source.login_url||!source.site_username||!source.site_password_enc)return{configured:false,results:[],error:null}; let password; try{password=decrypt(source.site_password_enc);}catch(e){return{configured:true,results:[],error:`Credential decryption failed: ${e.message}`};} let browser,context; try{browser=await chromium.launch({headless:true});context=await browser.newContext({viewport:{width:1440,height:1000}});const page=await context.newPage();page.setDefaultTimeout(Number(cfg.timeout_ms)||15000);const apiBodies=[];const apiRequests=[];page.on('request',request=>{if(/GetHotelsJson\.aspx/i.test(request.url()))apiRequests.push({url:request.url(),method:request.method()});});page.on('response',async response=>{if(!/GetHotelsJson\.aspx/i.test(response.url())||response.status()!==200)return;try{const body=await response.text();if(body&&body.length>20)apiBodies.push(body);}catch{}});await page.goto(source.login_url,{waitUntil:'commit',timeout:60000});await page.waitForTimeout(Number(cfg.initial_wait_ms)||3000);await blocked(page);await login(page,source,password,cfg);await openSearchPage(page,cfg);const frame=await searchForm(page);if(!frame)throw new Error(`Hadaf hotel search form could not be detected. URL: ${page.url()}`);await fillSearch(frame,search);const before=apiBodies.length;const clicked=await clickSearch(frame,cfg);const end=Date.now()+(Number(cfg.search_wait_ms)||45000);while(Date.now()<end&&apiBodies.length===before)await page.waitForTimeout(500);const rendered=await extractRenderedRates(context,cfg);let results=rendered.map(r=>({...r,supplier:'Hadaf Holidays',supplierRoomCode:'',rateFrom:'',rateTo:''}));if(!results.length)results=apiBodies.flatMap(b=>{try{return parseHadafRates(JSON.parse(b),cfg);}catch{return[];}});if(!results.length)throw new Error(`Hadaf search completed but no priced rates were extracted. GetHotelsJson responses: ${apiBodies.length}. Requests: ${JSON.stringify(apiRequests)}. Search control: ${clicked}. URL: ${page.url()}`);return{configured:true,results:results.slice(0,Number(cfg.max_results)||500),error:null};}catch(e){return{configured:true,results:[],error:e.name==='TimeoutError'?`Hadaf browser timed out at Playwright operation: ${e.message}`:e.message};}finally{if(context)await context.close().catch(()=>{});if(browser)await browser.close().catch(()=>{});}}
module.exports={searchHadafSource,healthHadafSource};
