let tempToken=null,currentSearchId=null,currentSources=[];const $=id=>document.getElementById(id),show=id=>$(id).classList.remove('hidden'),hide=id=>$(id).classList.add('hidden');
async function api(path,opts={}){const res=await fetch(path,{headers:{'Content-Type':'application/json'},credentials:'include',...opts});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||'Request failed');return data}
$('login-form').addEventListener('submit',async e=>{e.preventDefault();$('login-error').textContent='';try{const d=await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:$('username').value,password:$('password').value})});tempToken=d.tempToken;hide('login-view');show('twofa-view')}catch(err){$('login-error').textContent=err.message}});
$('twofa-form').addEventListener('submit',async e=>{e.preventDefault();$('twofa-error').textContent='';try{const d=await api('/api/auth/verify-2fa',{method:'POST',body:JSON.stringify({tempToken,code:$('twofa-code').value})});hide('twofa-view');$('who').textContent=d.username;show('app-view');applyRole(d.role);loadSources()}catch(err){$('twofa-error').textContent=err.message}});
$('logout-btn').addEventListener('click',async()=>{await api('/api/auth/logout',{method:'POST'});location.reload()});
function applyRole(role){document.querySelectorAll('.admin-only').forEach(el=>el.classList.toggle('hidden',!['ADMIN','SUPER_ADMIN'].includes(role)))}
async function loadSources(){try{currentSources=await api('/api/sources');const tbody=document.querySelector('#sources-table tbody'),select=$('c-source');tbody.innerHTML='';select.innerHTML='';currentSources.forEach(s=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${s.name}</td><td><a href="${s.login_url}" target="_blank" rel="noopener noreferrer">${s.login_url}</a></td><td>${s.site_username||''}</td><td>Protected</td><td><button data-id="${s.id}" class="del-source">Remove</button></td>`;tbody.appendChild(tr);const opt=document.createElement('option');opt.value=s.id;opt.textContent=s.name;select.appendChild(opt)});document.querySelectorAll('.del-source').forEach(btn=>btn.addEventListener('click',async()=>{if(confirm('Remove this source?')){await api('/api/sources/'+btn.dataset.id,{method:'DELETE'});loadSources()}}))}catch(err){console.error(err)}}
$('source-form').addEventListener('submit',async e=>{e.preventDefault();$('source-error').textContent='';try{await api('/api/sources',{method:'POST',body:JSON.stringify({name:$('src-name').value,login_url:$('src-url').value,deep_link_template:$('src-template').value,site_username:$('src-username').value,site_password:$('src-password').value})});e.target.reset();await loadSources()}catch(err){$('source-error').textContent=err.message}});
$('search-form').addEventListener('submit',async e=>{e.preventDefault();try{const d=await api('/api/search',{method:'POST',body:JSON.stringify({destination:$('s-destination').value,checkin:$('s-checkin').value,checkout:$('s-checkout').value,guests:Number($('s-guests').value),rooms:Number($('s-rooms').value),board:$('s-board').value})});currentSearchId=d.searchId;show('results-section');const list=$('links-list');list.innerHTML='';d.links.forEach(l=>{const a=document.createElement('a');a.href=l.url;a.target='_blank';a.rel='noopener noreferrer';a.textContent=l.name;list.appendChild(a)});loadComparisons()}catch(err){alert(err.message)}});
async function loadComparisons(){if(!currentSearchId)return;const rows=await api('/api/comparisons/'+currentSearchId),tbody=document.querySelector('#compare-table tbody');tbody.innerHTML='';$('comparison-empty').classList.toggle('hidden',rows.length>0);rows.forEach(r=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${r.source_name}</td><td>${r.price}</td><td>${r.room_type||''}</td><td>${r.notes||''}</td><td><button data-id="${r.id}" class="del-comp">x</button></td>`;tbody.appendChild(tr)});document.querySelectorAll('.del-comp').forEach(btn=>btn.addEventListener('click',async()=>{await api('/api/comparisons/'+btn.dataset.id,{method:'DELETE'});loadComparisons()}))}
$('compare-form').addEventListener('submit',async e=>{e.preventDefault();if(!currentSearchId)return;try{await api('/api/comparisons',{method:'POST',body:JSON.stringify({search_id:currentSearchId,source_id:Number($('c-source').value),price:$('c-price').value,room_type:$('c-room').value,notes:$('c-notes').value})});e.target.reset();loadComparisons()}catch(err){alert(err.message)}});
(async()=>{try{const me=await api('/api/auth/me');$('who').textContent=me.username;hide('login-view');show('app-view');applyRole(me.role);loadSources()}catch{}})();
