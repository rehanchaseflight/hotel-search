let tempToken=null,currentSearchId=null,currentSources=[];
const $=id=>document.getElementById(id),show=id=>$(id).classList.remove('hidden'),hide=id=>$(id).classList.add('hidden');
async function api(path,opts={}){const res=await fetch(path,{headers:{'Content-Type':'application/json'},credentials:'include',...opts});const data=await res.json().catch(()=>({}));if(!res.ok)throw new Error(data.error||'Request failed');return data}
$('login-form').addEventListener('submit',async e=>{e.preventDefault();$('login-error').textContent='';try{const d=await api('/api/auth/login',{method:'POST',body:JSON.stringify({username:$('username').value,password:$('password').value})});tempToken=d.tempToken;hide('login-view');show('twofa-view')}catch(err){$('login-error').textContent=err.message}});
$('twofa-form').addEventListener('submit',async e=>{e.preventDefault();$('twofa-error').textContent='';try{const d=await api('/api/auth/verify-2fa',{method:'POST',body:JSON.stringify({tempToken,code:$('twofa-code').value})});hide('twofa-view');$('who').textContent=d.username;show('app-view');applyRole(d.role);loadSources();loadSupplierHealth()}catch(err){$('twofa-error').textContent=err.message}});
$('logout-btn').addEventListener('click',async()=>{await api('/api/auth/logout',{method:'POST'});location.reload()});
function applyRole(role){document.querySelectorAll('.admin-only').forEach(el=>el.classList.toggle('hidden',!['ADMIN','SUPER_ADMIN'].includes(role)))}
async function loadSources(){try{currentSources=await api('/api/sources');const tbody=document.querySelector('#sources-table tbody');tbody.innerHTML='';currentSources.forEach(s=>{const tr=document.createElement('tr');tr.innerHTML=`<td>${s.name}</td><td><a href="${s.login_url}" target="_blank" rel="noopener noreferrer">${s.login_url}</a></td><td>Browser / B2B login</td><td>Encrypted credentials</td><td><button data-id="${s.id}" class="del-source">Remove</button></td>`;tbody.appendChild(tr)});document.querySelectorAll('.del-source').forEach(btn=>btn.addEventListener('click',async()=>{if(confirm('Remove this source?')){try{await api('/api/sources/'+btn.dataset.id,{method:'DELETE'});await loadSources();await loadSupplierHealth()}catch(err){alert(err.message)}}}))}catch(err){console.error(err)}}
async function loadSupplierHealth(){const box=$('dashboard-supplier-status');if(!box)return;try{box.innerHTML='<div class="searching-status">Checking Hadaf B2B login…</div>';const statuses=await api('/api/supplier-health');renderSupplierStatus(statuses,'dashboard-supplier-status')}catch(err){box.innerHTML=`<p class="error">Hadaf health check failed: ${err.message}</p>`}}
$('source-form').addEventListener('submit',async e=>{e.preventDefault();$('source-error').textContent='';$('source-error').className='error';const button=e.target.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Saving...';try{let browserConfig={};if($('src-browser-config').value.trim())browserConfig=JSON.parse($('src-browser-config').value);await api('/api/sources',{method:'POST',body:JSON.stringify({name:$('src-name').value.trim(),login_url:$('src-url').value.trim(),connector_type:'browser',agent_code:$('src-agent-code').value.trim(),site_username:$('src-username').value.trim(),site_password:$('src-password').value,browser_config:browserConfig})});e.target.reset();await loadSources();await loadSupplierHealth();$('source-error').textContent='Supplier login credentials saved securely.';$('source-error').className='ok'}catch(err){$('source-error').textContent=err.message}finally{button.disabled=false;button.textContent='Save Supplier'}});
const supplierOrder=['public-market','rezlive','b2b-bookings','nowworld','saltours','arabian-oryx','leamigo','locanda','babylon-holidays','peaceland-holidays','holiday-begin','ratehawk','wanderbeds','mawasim','webbeds','tripovo','expedia-taap','gatetours','rateloc','anjum-hotel-makkah'];
function renderSupplierStatus(statuses,targetId='supplier-status'){const box=$(targetId);if(!box)return;box.innerHTML='';const map=new Map((statuses||[]).map(s=>[s.id,s]));const live=[...map.values()].filter(s=>s.status==='live'||(s.configured&&s.ok));const offline=[...map.values()].filter(s=>s.status==='offline'||(s.configured&&!s.ok));const notConfigured=[...map.values()].filter(s=>!s.configured&&s.status!=='offline');const summary=document.createElement('div');summary.className='supplier-summary';summary.innerHTML=`<strong>${live.length} live</strong> · <strong>${offline.length} offline</strong> · <strong>${notConfigured.length} not configured</strong>`;box.appendChild(summary);const grid=document.createElement('div');grid.className='supplier-grid';[...supplierOrder,...(statuses||[]).filter(s=>!supplierOrder.includes(s.id)).map(s=>s.id)].forEach(id=>{const s=map.get(id);if(!s)return;const card=document.createElement('div');card.className='supplier-status-card';let label='Not configured',cls='waiting';if(s.status==='live'||(s.configured&&s.ok)){label='Live';cls='live'}else if(s.status==='offline'||(s.configured&&!s.ok)){label='Offline';cls='error'}card.innerHTML=`<span>${s.name}</span><span class="supplier-badge ${cls}">${label}</span>${s.error?`<small>${s.error}</small>`:''}`;grid.appendChild(card)});box.appendChild(grid)}
function addCell(tr,value,cls=''){const td=document.createElement('td');td.textContent=value||'—';if(cls)td.className=cls;tr.appendChild(td)}
function renderResults(results,statuses){renderSupplierStatus(statuses);const box=$('live-results');if(!box)return;box.innerHTML='';const rates=(Array.isArray(results)?results:[]).filter(r=>Number.isFinite(Number(r.price))&&Number(r.price)>0);const count=document.createElement('div');count.className='results-count';count.textContent=rates.length?`${rates.length} live priced rate${rates.length===1?'':'s'} found`:'No live priced rates returned';box.appendChild(count);if(!rates.length){const p=document.createElement('p');p.className='hint';p.textContent='No priced hotel rates were returned by the live suppliers.';box.appendChild(p);return}rates.sort((a,b)=>Number(a.price)-Number(b.price));const wrap=document.createElement('div');wrap.style.overflowX='auto';const table=document.createElement('table');table.className='rate-results-table';const thead=document.createElement('thead');const hrow=document.createElement('tr');['Hotel','Room Category','View','Board','Cancellation','Price','Availability','Supplier'].forEach(h=>{const th=document.createElement('th');th.textContent=h;hrow.appendChild(th)});thead.appendChild(hrow);table.appendChild(thead);const tbody=document.createElement('tbody');rates.forEach(r=>{const tr=document.createElement('tr');addCell(tr,r.hotel);addCell(tr,r.room);addCell(tr,r.view);addCell(tr,r.board);addCell(tr,r.cancellation);addCell(tr,`${r.currency||'USD'} ${Number(r.price).toFixed(2)}`,'price-cell');addCell(tr,r.availability||'Available');addCell(tr,r.supplier||'Supplier');tbody.appendChild(tr)});table.appendChild(tbody);wrap.appendChild(table);box.appendChild(wrap)}
$('search-form').addEventListener('submit',async e=>{e.preventDefault();const button=e.target.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Searching all suppliers...';show('results-section');$('search-summary').textContent=`Searching ${$('s-destination').value} • ${$('s-checkin').value} to ${$('s-checkout').value}`;$('supplier-status').innerHTML='<div class="searching-status">Searching configured suppliers…</div>';$('live-results').innerHTML='<p class="hint">Waiting for live responses…</p>';try{const d=await api('/api/search',{method:'POST',body:JSON.stringify({destination:$('s-destination').value.trim(),checkin:$('s-checkin').value,checkout:$('s-checkout').value,guests:Number($('s-guests').value),rooms:Number($('s-rooms').value),board:$('s-board').value})});currentSearchId=d.searchId;$('search-summary').textContent=`${$('s-destination').value} • ${$('s-checkin').value} to ${$('s-checkout').value} • ${$('s-guests').value} guests • ${$('s-rooms').value} room${Number($('s-rooms').value)===1?'':'s'} • ${$('s-board').selectedOptions[0].text}`;renderResults(d.results,d.connectorStatuses)}catch(err){$('supplier-status').innerHTML='';$('live-results').innerHTML=`<p class="error">${err.message}</p>`}finally{button.disabled=false;button.textContent='Search'}});
(async()=>{try{const me=await api('/api/auth/me');$('who').textContent=me.username;hide('login-view');show('app-view');applyRole(me.role);loadSources();loadSupplierHealth()}catch{}})();
